# Should be identical to .gitignore
.env
node_modules
.idea
data
stacks
tmp
/private

# Docker extra
docker
# frontend/ is NOT ignored: the frontend_build stage compiles it inside the image.
# frontend-dist is ignored so a stale local build cannot shadow that stage's output.
frontend-dist
.editorconfig
.eslintrc.cjs
.git
.gitignore
README.md
